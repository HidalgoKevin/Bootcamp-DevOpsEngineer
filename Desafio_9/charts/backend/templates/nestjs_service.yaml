apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-nestjs-backend
spec:
  type: ClusterIP
  selector:
    app: nestjs
  ports:
  - protocol: TCP
    port: {{ .Values.backend.port }}
    targetPort: {{ .Values.backend.targetport }}
