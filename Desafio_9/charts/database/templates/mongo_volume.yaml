apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.database.name_volume }}
spec:
  accessModes:
    - {{ .Values.database.accessModes }}
  resources:
    requests:
      storage: {{ .Values.database.storage }}